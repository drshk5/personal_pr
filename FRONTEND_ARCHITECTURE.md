# 🎨 Frontend Architecture - Visual Guide

## Component Hierarchy

```
┌─────────────────────────────────────────────────────────────┐
│                    CRM Frontend Application                  │
└─────────────────────────────────────────────────────────────┘
                              │
                ┌─────────────┼─────────────┐
                │             │             │
         ┌──────▼──────┐  ┌──▼──────┐  ┌──▼─────────────┐
         │  Activities  │  │ Accounts │  │  Dashboard     │
         │   Page       │  │  Detail  │  │  Page          │
         └──────┬───────┘  └────┬─────┘  └──┬─────────────┘
                │               │           │
         ┌──────▼──────┐  ┌─────▼─────────┐ │
         │ActivityList  │  │Activity Tab   │ │
         │Enhanced      │  │+ Timeline     │ │
         └──────┬───────┘  └─────┬─────────┘ │
                │                │           │
                │         ┌──────▼───────────▼───┐
                │         │ Dashboard Widgets:   │
                │         │ • Today's Tasks      │
                │         │ • Overdue Activities │
                │         │ • My Count           │
                │         │ • Team Overdue       │
                │         └──────────────────────┘
                │
         ┌──────▼──────────────────┐
         │   Shared Hooks & Services│
         │  ┌────────────────────┐ │
         │  │use-activities-     │ │
         │  │extended.ts         │ │
         │  ├────────────────────┤ │
         │  │activity-extended-  │ │
         │  │service.ts          │ │
         │  └────────────────────┘ │
         └──────────────────────────┘
                │
         ┌──────▼──────────────────┐
         │   Backend API            │
         │  ──────────────────────  │
         │  /api/crm/activities     │
         │  ├── GET /               │
         │  ├── POST /              │
         │  ├── GET /{id}           │
         │  ├── PUT /{id}           │
         │  ├── DELETE /{id}        │
         │  ├── PATCH /{id}/status  │
         │  ├── PATCH /{id}/assign  │
         │  ├── POST /bulk-assign   │
         │  ├── POST /bulk-status   │
         │  ├── POST /bulk-delete   │
         │  ├── GET /today          │
         │  ├── GET /my-activities  │
         │  ├── GET /overdue        │
         │  ├── GET /upcoming       │
         │  └── GET /entity/{}/{id} │
         └──────────────────────────┘
```

---

## Data Flow

### Creating an Activity

```
┌─────────────────────────┐
│  ActivityForm Component  │
│  (modal/dialog)         │
└────────────┬────────────┘
             │
             ▼
     ┌───────────────┐
     │useCreateActivity│ (Hook with mutation)
     └────────┬──────┘
             │
             ▼
┌──────────────────────────┐
│ activityExtendedService  │
│ .createActivity(dto)     │
└────────────┬─────────────┘
             │
             ▼
    ┌────────────────────┐
    │  ApiService.post   │
    │  /api/crm/         │
    │  activities        │
    └────────┬───────────┘
             │
             ▼
    ┌────────────────────┐
    │  Backend API       │
    │  Creates Activity  │
    └────────┬───────────┘
             │
             ▼
    ┌────────────────────┐
    │  Invalidate Queries│ (React Query)
    │  Update UI         │
    └────────┬───────────┘
             │
             ▼
    ┌────────────────────┐
    │  Show Toast        │
    │  Close Modal       │
    │  Refetch List      │
    └────────────────────┘
```

### Displaying Activities

```
┌─────────────────────┐
│  ActivityList Page  │
└────────┬────────────┘
         │
         ▼
    ┌─────────────────────────────┐
    │ useActivitiesExtended()     │
    │ (React Query hook)          │
    └────────┬────────────────────┘
             │
             ├─ fetch activities
             │  with filters/params
             │
             ▼
    ┌─────────────────────────────┐
    │ activityExtendedService     │
    │ .getActivities(params)      │
    └────────┬────────────────────┘
             │
             ▼
    ┌─────────────────────────────┐
    │ ApiService.getWithMeta      │
    │ /api/crm/activities?...     │
    └────────┬────────────────────┘
             │
             ▼
    ┌─────────────────────────────┐
    │ Backend API Returns:        │
    │ {                           │
    │   data: [Activity[]],       │
    │   meta: { totalCount, ... } │
    │ }                           │
    └────────┬────────────────────┘
             │
             ▼
    ┌─────────────────────────────┐
    │ React Query Caches Data     │
    │ Triggers re-render          │
    └────────┬────────────────────┘
             │
             ▼
    ┌─────────────────────────────┐
    │ DataTable Component         │
    │ Renders rows with:          │
    │ • Type icon                 │
    │ • Subject                   │
    │ • Status                    │
    │ • Actions                   │
    └─────────────────────────────┘
```

---

## Component Relationships

```
┌──────────────────────────────────────────────────────────┐
│                    ApplicationShell                       │
│  (App.tsx with theme provider, auth, toaster)           │
└────────────────────┬─────────────────────────────────────┘
                     │
        ┌────────────┼────────────┐
        │            │            │
   ┌────▼────┐  ┌────▼────┐  ┌──▼─────┐
   │ Sidebar  │  │ Header   │  │ Routes │
   └─────────┘  └──────────┘  └──┬─────┘
                                  │
        ┌─────────────────────────┼─────────────────────────┐
        │                         │                         │
   ┌────▼────────┐         ┌──────▼──────┐        ┌────────▼────┐
   │ ActivityList │         │AccountDetail │       │Dashboard    │
   │Enhanced     │         │Page         │       │Page        │
   ├─────────────┤         ├─────────────┤       ├────────────┤
   │ Search      │         │ Tabs:       │       │ Cards:     │
   │ Filter      │         │ - Overview  │       │ • Today    │
   │ Pagination  │         │ - Activities│       │ • Overdue  │
   │ Sort        │         │ - Timeline  │       │ • My Count │
   │ Bulk delete │         │ - Related   │       │ • Team     │
   │ Details     │         │             │       │            │
   └────┬────────┘         └──────┬──────┘       └────────────┘
        │                         │
        └─────────────┬───────────┘
                      │
        ┌─────────────▼──────────────┐
        │   Shared Components:        │
        │ • ActivityForm              │
        │ • ActivityTypeIcon          │
        │ • DashboardActivityWidgets  │
        └─────────────┬──────────────┘
                      │
        ┌─────────────▼──────────────┐
        │  Hooks & Services:          │
        │ • useActivitiesExtended()   │
        │ • useCreateActivity()       │
        │ • activityExtendedService   │
        └────────────────────────────┘
```

---

## Theme System

```
┌─────────────────────────────────────────────────────────┐
│           App Theme Provider (Tailwind CSS)             │
│  (Switches between "light" and "dark" classes)          │
└────────┬────────────────────────────────────────────────┘
         │
         ├─── Light Theme (Default)
         │    └─ bg-white
         │    └─ text-slate-900
         │    └─ border-slate-200
         │
         └─── Dark Theme (:root.dark)
              └─ dark:bg-slate-900
              └─ dark:text-slate-100
              └─ dark:border-slate-800
              │
              ├─ Components automatically apply both
              ├─ All colors have dark: variants
              └─ No CSS variable changes needed
```

### Examples of Dark Mode CSS:

```tsx
// Input with dark mode
<input className="
  bg-white dark:bg-slate-800
  text-slate-900 dark:text-slate-100
  border-slate-200 dark:border-slate-700
  focus:dark:ring-blue-600
" />

// Card with dark mode
<Card className="
  dark:bg-slate-900
  dark:border-slate-800
">
  <CardHeader>
    <CardTitle className="dark:text-slate-100">Title</CardTitle>
  </CardHeader>
</Card>

// Hover state with dark mode
<Button className="
  hover:bg-slate-100 dark:hover:bg-slate-800
  hover:border-slate-300 dark:hover:border-slate-600
" />
```

---

## State Management

```
┌──────────────────────────────────────────────────────────┐
│           React Query (TanStack Query)                   │
│  Global cache management for server state                │
└──────────────────────────────┬───────────────────────────┘
                               │
        ┌──────────────────────┼──────────────────────┐
        │                      │                      │
    ┌───▼──────┐         ┌────▼────┐         ┌──────▼────┐
    │ useQuery  │         │useMutation│        │invalidate │
    │(SELECTs)  │         │(CUD ops)  │        │(refresh)  │
    ├────┬──────┤         ├────┬──────┤        └───────────┘
    │ get       │         │post     │
    │ list      │         │put      │
    │ detail    │         │delete   │
    │ today     │         │patch    │
    │ overdue   │         └────┬────┘
    │ my        │              │
    │ upcoming  │              ▼
    └────┬──────┘      ┌──────────────┐
         │             │ Toast        │
         │             │ Notification │
         │             └──────────────┘
         │
         ▼
    ┌──────────────────┐
    │ Component State  │
    │                  │
    │ useState for:    │
    │ • Modal open     │
    │ • Filters        │
    │ • Sort           │
    │ • Selected       │
    │ • UI feedback    │
    └──────────────────┘
```

---

## API Response Types

### List Response
```tsx
interface ActivityListResponse {
  data: ActivityListDto[];
  meta: {
    totalCount: number;
    totalPages: number;
    pageNumber: number;
    pageSize: number;
  };
}
```

### Single Activity
```tsx
interface ActivityListDto {
  strActivityGUID: string;
  strActivityType: string;
  strSubject: string;
  strDescription?: string;
  dtScheduledOn?: string;
  dtCompletedOn?: string;
  intDurationMinutes?: number;
  strOutcome?: string;
  strAssignedToGUID?: string;
  strAssignedToName?: string;
  strCreatedByGUID?: string;
  strCreatedByName: string;
  dtCreatedOn: string;
  bolIsActive: boolean;
  links: ActivityLinkDto[];
}
```

---

## Performance Optimizations

```
┌─────────────────────────────────────────────────────┐
│          Performance Features                       │
├─────────────────────────────────────────────────────┤
│ ✅ React.memo on list items                         │
│ ✅ useCallback for event handlers                   │
│ ✅ useMemo for computed values                      │
│ ✅ React Query caching (30-60sec)                   │
│ ✅ Pagination (20-100 items per page)               │
│ ✅ Lazy loading on scroll (optional)                │
│ ✅ Code splitting (route-based)                     │
│ ✅ Image optimization (Lucide icons)                │
│ ✅ CSS-in-JS (Tailwind - no runtime)                │
│ ✅ TypeScript strict mode                           │
└─────────────────────────────────────────────────────┘
```

---

## Browser Compatibility

```
┌─────────────────────────────────────────────────────┐
│      Supported Browsers                             │
├─────────────────────────────────────────────────────┤
│ ✅ Chrome/Edge 90+                                  │
│ ✅ Firefox 88+                                      │
│ ✅ Safari 14+                                       │
│ ✅ Mobile Safari 14+                                │
│ ✅ Mobile Chrome 90+                                │
│ ✅ Dark Mode Support (all modern browsers)          │
│ ✅ Responsive Design (320px - 4K)                   │
└─────────────────────────────────────────────────────┘
```

---

## Error Handling Flow

```
┌─────────────────────────────────────────────┐
│          Component / API Call               │
└────────────────┬────────────────────────────┘
                 │
         ┌───────┴────────┐
         │                │
    ┌────▼────┐      ┌────▼────┐
    │ Success  │      │  Error  │
    └────┬─────┘      └────┬────┘
         │                 │
         │          ┌──────▼──────┐
         │          │ Catch Error │
         │          └──────┬──────┘
         │                 │
         │          ┌──────▼──────────────┐
         │          │ Extract Message &   │
         │          │ Log to Console      │
         │          └──────┬─────────────┘
         │                 │
         │          ┌──────▼──────────────┐
         │          │ Show Error Toast    │
         │          │ (red background)    │
         │          └──────┬─────────────┘
         │                 │
         ▼                 ▼
    ┌──────────────────────────────┐
    │   Component Display State    │
    │ • isLoading                  │
    │ • error message              │
    │ • retry button (optional)    │
    └──────────────────────────────┘
```

---

## Responsive Breakpoints

```
┌──────────────────────────────────────────────┐
│        Tailwind Responsive Breakpoints       │
├──────────────────────────────────────────────┤
│ Mobile:   <640px   (sm)                      │
│ Tablet:   640-1024px (md, lg)                │
│ Desktop:  >1024px  (xl, 2xl)                 │
│                                              │
│ Grid Layout:                                 │
│ • Mobile:  1 column grid                     │
│ • Tablet:  2 column grid (md:)               │
│ • Desktop: 4 column grid (lg:)               │
│                                              │
│ List Display:                                │
│ • Mobile:  Stacked cards                     │
│ • Tablet:  2 items per row                   │
│ • Desktop: Table with scrolling              │
└──────────────────────────────────────────────┘
```

---

## Feature Comparison

```
┌──────────────────────────────────────────────────────────┐
│         Feature Availability by Component               │
├─────────────────┬────────┬────────┬──────────┬──────────┤
│ Feature         │ List   │ Tab    │ Timeline │Dashboard │
├─────────────────┼────────┼────────┼──────────┼──────────┤
│ Search          │   ✅   │   ✅   │    ❌    │    ❌    │
│ Filter          │   ✅   │   ✅   │    ❌    │    ❌    │
│ Sort            │   ✅   │   ✅   │    ✅    │    ❌    │
│ Pagination      │   ✅   │   ✅   │    ❌    │    ❌    │
│ Bulk Delete     │   ✅   │   ✅   │    ❌    │    ❌    │
│ Create          │   ✅   │   ✅   │    ❌    │    ❌    │
│ Edit Detail     │   ✅   │   ✅   │    ✅    │    ✅    │
│ Status Badge    │   ✅   │   ✅   │    ✅    │    ✅    │
│ Action Menu     │   ✅   │   ❌   │    ❌    │    ❌    │
│ Statistics      │   ❌   │   ✅   │    ❌    │    ✅    │
│ Icons           │   ✅   │   ✅   │    ✅    │    ✅    │
│ Dark Theme      │   ✅   │   ✅   │    ✅    │    ✅    │
│ Responsive      │   ✅   │   ✅   │    ✅    │    ✅    │
└─────────────────┴────────┴────────┴──────────┴──────────┘
```

---

## Summary

Your frontend architecture is:
- ✅ **Modular**: Components can be reused independently
- ✅ **Typed**: Full TypeScript coverage
- ✅ **Themed**: Complete dark/light mode support
- ✅ **Performant**: Optimized caching and rendering
- ✅ **Scalable**: Easy to add new features
- ✅ **Accessible**: Keyboard navigation, ARIA labels
- ✅ **Mobile-First**: Responsive design
- ✅ **State-Managed**: React Query for server state
- ✅ **Error-Handled**: Comprehensive error catching
- ✅ **User-Friendly**: Loading/error/empty states

Ready for production deployment! 🚀
